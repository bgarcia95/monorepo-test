import {View} from 'react-native';
import React from 'react';
import {ReusableButton} from 'common-components-1/components/ResusableButton';

const App = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <ReusableButton />
    </View>
  );
};

export default App;
